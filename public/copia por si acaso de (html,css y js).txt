
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Keyon Access SystemV3</title>
<link rel="stylesheet" href="style.css?v=1.20">
<link rel="stylesheet" href="mini-overlay-futurista.css?v=2">
<link rel="icon" href="assets/favicon.ico">
</head>
<body>

<h1>Keyon Access System</h1>

<!-- Overlay selección de rol -->
<div id="overlay-rol">
  <h2>🔐 Selecciona tu rol</h2>
  <div class="roles">
    <button class="rol-btn" data-rol="Alumno">👨‍🎓 Alumno</button>
    <button class="rol-btn" data-rol="Profesor">👩‍🏫 Profesor</button>
    <button class="rol-btn" data-rol="Admin">👑 Admin</button>
  </div>

  <div id="password-container" style="display:none;">
    <input type="password" id="password-rol" placeholder="Ingresa tu contraseña">
    <button id="btn-entrar">Entrar</button>
    <div id="error-rol"></div>
  </div>
</div>

<!-- Overlay de escaneo -->
<!-- Overlay de escaneo -->
<div id="overlay" class="overlay">
  <div id="overlay-content">
    <h2 id="overlay-titulo">📷 Escaneando QR...</h2>
    <div id="video-container">
      <div id="video" style="width:100%; max-width:400px; height:300px; border: 2px solid #00aaff; border-radius:12px; margin:auto;"></div>
    </div>
    <p id="prof-scan-status">Apunta el QR del alumno</p>
  </div>
</div>

<!-- Overlay de mensajes (éxito, error, advertencia, info) -->
<div id="overlay-msg" class="overlay" style="display:none;">
  <div class="overlay-content">
    <h2 id="overlay-msg-titulo">✅ Escaneo exitoso</h2>
    <p id="overlay-msg-info">Alumno registrado correctamente.</p>
  </div>
</div>


<!-- Panel Alumno -->
<!-- Tu HTML del panel alumno -->
<section id="panel-alumno" style="display:none;">
  <h3>Generador QR Alumno</h3>
  <input type="text" id="alumno-nombre" placeholder="Nombre (s)">
  <input type="text" id="alumno-apellidos" placeholder="Apellidos">
  <input type="text" id="alumno-grado" placeholder="Grado y Grupo">
  <input type="text" id="alumno-control" placeholder="N/O de Control Escolar">
  <input type="text" id="alumno-whatsapp" placeholder="WhatsApp">

  <div id="alumno-qrcode"></div>
  <svg id="alumno-barcode"></svg>

  <button id="btnAlumnoQRCamara">Generar QR cámara</button>
  <button id="btnAlumnoQRLector">Generar código de barras</button>
  <button id="alumno-descargar">⬇️ Download QR/Barcode</button>


<!-- 🔒 Botón oculto para abrir Panel CBTis -->
<!-- ⚙️ Botón oculto para abrir el Panel CBTis -->
<button id="btnAbrirPanelCBTis" 
  style="display:none; position:fixed; top:10px; right:10px; 
         background:rgba(0,0,0,0.2); color:white; border:none; 
         border-radius:50%; width:40px; height:40px; 
         cursor:pointer; font-size:20px; z-index:9999; 
         backdrop-filter:blur(3px); transition:opacity .3s, transform .2s;">
  ⚙️
</button>

<script>
document.addEventListener("DOMContentLoaded", () => {
  const btn = document.getElementById("btnAbrirPanelCBTis");

  // 💫 Efecto de hover sutil
  btn.addEventListener("mouseenter", () => btn.style.opacity = "1");
  btn.addEventListener("mouseleave", () => btn.style.opacity = "0.6");

  // 🔐 Atajo secreto: Ctrl + Alt + C
  document.addEventListener("keydown", e => {
    if (e.ctrlKey && e.altKey && e.key.toLowerCase() === "c") {
      if (btn.style.display === "none") {
        btn.style.display = "block";
        btn.style.opacity = "0.6";
        btn.style.transform = "scale(1)";
      } else {
        btn.style.display = "none";
      }
    }
  });

  // 🖱️ Click para abrir el panel CBTis
  btn.addEventListener("click", () => {
    try {
      if (typeof InitPanelCBTis === "function") {
        InitPanelCBTis();
        console.log("✅ Panel CBTis abierto desde botón secreto");
      } else {
        console.warn("⚠️ Función InitPanelCBTis no encontrada");
      }
    } catch (err) {
      console.error("❌ Error al abrir Panel CBTis:", err);
    }
  });
});
</script>
</section>


<!-- Panel Profesor -->
<section id="panel-profesor" style="display:none;">
  <h3>Generador QR Profesor</h3>
  <input type="text" id="prof-nombre" placeholder="Nombre (s)">
  <input type="text" id="prof-apellidos" placeholder="Apellidos">
  <input type="text" id="prof-materias" placeholder="Materias (Sin espacios)">
  <input type="text" id="prof-grupos" placeholder="Grupos (Sin espacios)">
  <input type="text" id="prof-telefono" placeholder="Teléfono (Sin espacios)">

  <!-- Contenedor QR -->
  <div id="prof-qrcode"></div>

  <!-- Contenedor para código de barras -->
  <svg id="prof-barcode"></svg>

  <button id="btnProfQRCamara">Generar QR cámara</button>
  <button id="btnProfQRLector">Generar código de barras</button>
  <button id="prof-descargar">⬇️ Descargar QR</button>


<!-- Mini-overlay de alumnos escaneados -->
<div id="overlay-mini" style="
  display:none;
  position:fixed;
  bottom:20px;
  right:20px;
  background:#fff;
  border:1px solid #333;
  padding:10px;
  border-radius:8px;
  max-width:250px;
  max-height:200px;
  overflow:auto;
  box-shadow:0 4px 8px rgba(0,0,0,0.2);
  font-size:0.9em;
  z-index:9999;
">
  <strong>Alumnos Escaneados:</strong>
  <ul id="lista-mini-alumnos" style="padding-left:15px; margin-top:5px;"></ul>
  <button id="btn-limpiar-overlay" style="margin-top:5px; font-size:0.8em;">🧹 Limpiar lista</button>
  <!-- Botón para finalizar clase y exportar Excel -->
<button id="btn-finalizar-clase" style="margin-top:10px; font-size:1em;">
  🏁 Finalizar Clase
</button>

</div>

  <!-- Escáner para profesor -->
  <h3>Escáner de alumnos</h3>
  <div id="qr-reader" style="width:400px; height:400px; border: 2px solid #000;"></div>
  <p id="prof-scan-status">Apunta el QR del alumno</p>

    <!-- Panel de mensajes principal -->
  <div id="panel-aviso">
    Por favor, Profesor registre su  QR o código de barras
  </div>

  <div class="modo-selector">
    <button onclick="activarModoCamara()">📷 Activar Camara</button>
    <button onclick="activarModoLector()">🔌 Activar lector Qr</button>
  </div>

  <!-- input lector: fuera del flujo -->
<input 
  id="input-lector" 
  type="text" 
  placeholder="Escanea código"
  autofocus
  style="
    width: 250px;
    height: 40px;
    font-size: 16px;
    padding: 6px 10px;
    border: 2px solid #0078d7;
    border-radius: 8px;
    outline: none;
  "
/>


</section>

<style>
  #input-lector {
  position: fixed;
  top: -100px; /* fuera de la vista */
  left: -100px;
  opacity: 0; /* invisible pero sigue en el DOM */
  pointer-events: none;
}
</style>



<script>
const input = document.getElementById("input-lector");

window.addEventListener("load", () => {
  input.focus();
  console.log("🔵 Lector listo");
});

// Refuerza foco cada 5 segundos por si el usuario hace clic fuera
setInterval(() => {
  if (document.activeElement !== input) {
    input.focus();
    console.log("🟢 Foco recuperado");
  }
}, 10000);

// Detecta cuando se escanea algo
input.addEventListener("input", (e) => {
  const codigo = e.target.value.trim();
  if (codigo.length > 3 && codigo.endsWith("\n")) return; // evitar falsos enter
});

// Detecta Enter (fin de escaneo)
input.addEventListener("keydown", (e) => {
  if (e.key === "Enter") {
    e.preventDefault();
    const codigo = input.value.trim();
    if (codigo !== "") {
      console.log("✅ Escaneado:", codigo);
      manejarCodigo(codigo);
      input.value = "";
    }
  }
});

function manejarCodigo(codigo) {
  // Aquí va tu lógica: buscar profesor, marcar asistencia, etc.
  console.log("📚 Procesando código:", codigo);
}
</script>




<!-- Panel Admin -->
<section id="panel-admin" style="display:none;">
  <h2>🛠 Panel Admin</h2>
  <button id="btn-export-usuarios">⬇ Exportar Usuarios CSV</button>
  <button id="btn-export-registros">⬇ Exportar Registros CSV</button>
  <div id="admin-log"></div>

  <div class="cards-container">
    <div class="card" id="card-alumnos">🎓 Total alumnos: 0</div>
    <div class="card" id="card-profesores">👩‍🏫 Total profesores: 0</div>
    <div class="card" id="card-registros-hoy">📋 Registros hoy: 0</div>
    <div class="card" id="card-usuarios-activos">✅ Usuarios activos: 0</div>
  </div>

<!-- Tabla de usuarios -->
<div class="table-container">
  <h3>Usuarios</h3>
  <input type="text" id="buscar-usuario" placeholder="Buscar usuario...">
  <table id="usuarios-table">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Apellidos</th>
        <th>Tipo</th>
        <th>Grado</th>        <!-- Solo alumnos -->
        <th>Grupo</th>        <!-- Solo profesores -->
        <th>Control</th>      <!-- Solo alumnos -->
        <th>Materias</th>     <!-- Solo profesores -->
        <th>Teléfono / WhatsApp</th>
        <th>Descargar QR</th>       <!-- Nueva columna -->
        <th>Descargar Código</th>   <!-- Nueva columna -->
        <th>Acciones</th>           <!-- Solo eliminar -->
      </tr>
    </thead>
    <tbody></tbody>
  </table>
</div>

<section id="exportarAccesosContainer">
  <h3>📊 Registros Entrada/Salida CBTis</h3>
  <label for="filtroFechaAccesos">Filtrar por fecha:</label>
  <input type="date" id="filtroFechaAccesos">
  
  <button id="btnExportarAccesos">
    📤 Exportar accesos a Excel
  </button>
</section>



  <!-- Tabla de registros -->
  <div class="table-container">
    <h3>Registros de asistencia</h3>
    <input type="date" id="filtro-fecha">
    <table id="registros-table">
      <thead>
        <tr>
          <th>Fecha</th>
          <th>Hora</th>
          <th>Tipo</th>
          <th>Nombre</th>
          <th>Profesor</th>
        </tr>
      </thead>
      <tbody></tbody>
    </table>
  </div>

  <div id="panel-admin-realtime" class="panel-admin">
  <h3>📊 Panel de Asistencia en Tiempo Real</h3>
  <canvas id="grafica-asistencia"></canvas>
  <div id="resumen-asistencia"></div>
</div>

<!-- =============================
     🔍 Panel de Clases Activas
============================= -->
<div id="panel-clases-activas">
  <h3>📚 Clases Activas</h3>
  <ul id="lista-clases-activas"></ul>
</div>

<!-- =============================
     📊 Gráfica del Aula Seleccionada
============================= -->
<div id="panel-grafica-aula" style="display:none;">
  <h3 id="titulo-grafica-aula">📈 Estadísticas del Aula</h3>
  <canvas id="grafica-aula" style="max-width:600px;"></canvas>
  <div id="resumen-aula"></div>
  <button id="btn-volver-general">⬅ Volver a vista general</button>
</div>



  <!-- Historial de escaneos en tiempo real -->
  <div id="historial-admin">
    <h4>📋 Últimos escaneos</h4>
    <ul id="historial-list-admin"></ul>
    <button id="export-historial-admin">⬇ Exportar historial</button>
  </div>
</section>





<!-- Historial QR -->
<section id="historial" style="display:none;">
  <h2>📋 Historial de Escaneos</h2>
  <ul id="historial-list"></ul>
</section>


<script src="https://cdn.jsdelivr.net/npm/jsbarcode@3.11.5/dist/JsBarcode.all.min.js"></script>
<script src="https://unpkg.com/html5-qrcode" type="text/javascript"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore-compat.js"></script>
<script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>


<script src="js/firebase-config.js"></script>
<script src="js/dom-elements.js"></script>

<script src="js/roles.js"></script>
<script src="js/audios.js"></script>

<script src="js/qr-barcode.js"></script>
<script src="js/overlays.js"></script>
<script src="js/iniciar-clase.js"></script>
<script src="js/scanner.js"></script>
<script src="js/procesar-qr.js"></script>

<script src="js/admin-panel.js?v=2.1"></script>
<script src="js/admin-grafica.js"></script>
<script src="js/acceso-cbtis.js"></script>

<script src="js/main.js"></script>

</body>
</html>





/* ======================== */
/* CONFIGURACIÓN GLOBAL & VARIABLES */
:root {
  /* Paleta Professional Deep Ocean */
  --bg-deep: #0f172a;
  --bg-surface: #1e293b;
  --primary: #38bdf8;       /* Cian Sky profesional */
  --primary-dark: #0284c7;  /* Azul sólido para profundidad */
  --accent: #6366f1;        /* Índigo elegante */
  --text-main: #f1f5f9;
  --text-muted: #94a3b8;
  
  /* Glass & 3D Vars */
  --glass-bg: rgba(30, 41, 59, 0.7);
  --glass-border: 1px solid rgba(255, 255, 255, 0.1);
  --glass-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
  --shadow-3d-btn: 0 6px 0 #0f172a, 0 15px 20px rgba(0,0,0,0.4);
  --ease-elastic: cubic-bezier(0.34, 1.56, 0.64, 1);
}

body {
  font-family: 'Poppins', sans-serif;
  /* Fondo complejo para resaltar el efecto cristal */
  background: radial-gradient(circle at 10% 20%, #1e293b, transparent 50%),
              radial-gradient(circle at 90% 80%, #0f172a, #020617);
  background-color: var(--bg-deep);
  color: var(--text-main);
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  min-height: 100vh;
  overflow-x: hidden;
}

/* ======================== */
/* TIPOGRAFÍA */
h1 {
  margin-top: 40px;
  font-size: 2.2rem;
  background: linear-gradient(180deg, #ffffff 0%, #94a3b8 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  text-align: center;
  font-weight: 800;
  letter-spacing: -1px;
  text-shadow: 0 10px 30px rgba(0,0,0,0.5);
}

h2, h3 {
  text-align: center;
  margin: 15px 0;
  color: var(--text-main);
  font-weight: 600;
  text-shadow: none;
}

/* ======================== */
/* BOTONES 3D TÁCTILES */
button {
  background: linear-gradient(145deg, #1e293b, #0f172a);
  color: var(--primary);
  border: 1px solid rgba(255, 255, 255, 0.05);
  border-radius: 12px;
  padding: 14px 26px;
  margin: 12px auto;
  cursor: pointer;
  font-size: 0.95rem;
  font-weight: 600;
  display: block;
  position: relative;
  transition: all 0.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  /* Efecto 3D físico */
  box-shadow: var(--shadow-3d-btn), inset 0 1px 0 rgba(255,255,255,0.1);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

button:hover {
  transform: translateY(-2px);
  background: linear-gradient(145deg, #263345, #141e2e);
  color: #fff;
  box-shadow: 0 8px 0 #0f172a, 0 20px 25px rgba(0,0,0,0.4), 0 0 20px rgba(56, 189, 248, 0.2);
  border-color: rgba(56, 189, 248, 0.3);
}

button:active {
  transform: translateY(4px);
  box-shadow: 0 2px 0 #0f172a, 0 2px 5px rgba(0,0,0,0.4);
}

/* ======================== */
/* INPUTS (INSET GLASS) */
input {
  padding: 16px;
  margin: 15px auto;
  border-radius: 12px;
  border: none;
  border-bottom: 2px solid rgba(255,255,255,0.1);
  width: 90%;
  max-width: 340px;
  display: block;
  box-sizing: border-box;
  font-size: 1rem;
  background: rgba(2, 6, 23, 0.6);
  box-shadow: inset 0 2px 5px rgba(0,0,0,0.5);
  color: #fff;
  backdrop-filter: blur(5px);
  transition: all 0.3s ease;
}

input:focus {
  outline: none;
  background: rgba(2, 6, 23, 0.8);
  border-bottom-color: var(--primary);
  box-shadow: inset 0 2px 5px rgba(0,0,0,0.6), 0 10px 30px -10px rgba(56, 189, 248, 0.15);
}

/* ======================== */
/* OVERLAY SELECCIÓN DE ROL */
#overlay-rol {
  position: fixed;
  top:0; left:0;
  width:100%; height:100%;
  background: rgba(15, 23, 42, 0.9);
  backdrop-filter: blur(15px);
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  z-index: 1000; /* Capa alta */
  text-align: center;
  animation: fadeIn 0.5s ease;
}

#overlay-rol .roles {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 25px;
  margin-top: 30px;
}

#overlay-rol button.rol-btn {
  min-width: 140px;
  height: 140px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-size: 1.1rem;
  background: rgba(30, 41, 59, 0.5);
  border-radius: 24px;
}

#overlay-rol button.rol-btn:hover {
  background: var(--primary);
  color: white;
}

#error-rol {
  color: #fb7185;
  margin-top: 20px;
  font-weight: 600;
  animation: fadeIn 0.5s ease;
}

/* ======================== */
/* PANEL DE AVISO */
#panel-aviso {
  transition: all 0.4s var(--ease-elastic);
  padding: 16px 24px;
  border-radius: 16px;
  font-weight: 600;
  font-size: 1rem;
  text-align: center;
  width: 90%;
  max-width: 480px;
  backdrop-filter: blur(12px);
  background: rgba(30, 41, 59, 0.4);
  border: var(--glass-border);
  margin-bottom: 20px;
  box-shadow: 0 4px 6px -1px rgba(0,0,0,0.1);
}

#panel-aviso.success {
  color: #ecfdf5;
  background: rgba(16, 185, 129, 0.2);
  border: 1px solid rgba(16, 185, 129, 0.3);
}

#panel-aviso.error {
  color: #fff1f2;
  background: rgba(225, 29, 72, 0.2);
  border: 1px solid rgba(225, 29, 72, 0.3);
}

/* ======================== */
/* PANELES TARJETA (GLASS SECTIONS) */
section {
  max-width: 640px;
  width: 92%;
  margin: 40px auto;
  border-radius: 24px;
  padding: 40px;
  
  /* Glassmorphism Premium */
  background: var(--glass-bg);
  backdrop-filter: blur(24px) saturate(180%);
  border: var(--glass-border);
  box-shadow: var(--glass-shadow);
  
  display: flex;
  flex-direction: column;
  align-items: center;
  opacity: 0;
  transform: translateY(30px) scale(0.95);
  animation: fadeSlideIn 0.6s var(--ease-elastic) forwards;
}

section h2 {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 1.6rem;
  color: var(--primary);
  text-shadow: 0 2px 10px rgba(56, 189, 248, 0.2);
}

/* ======================== */
/* CARDS RESUMEN */
.cards-container {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  margin: 20px 0;
  justify-content: center;
  width: 100%;
}

.card {
  flex: 1 1 160px;
  background: linear-gradient(135deg, rgba(255,255,255,0.05), rgba(255,255,255,0.01));
  padding: 20px;
  border-radius: 16px;
  border: var(--glass-border);
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  font-size: 1rem;
  text-align: center;
  color: var(--text-main);
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-4px);
  background: rgba(255,255,255,0.08);
  border-color: rgba(255,255,255,0.2);
}

/* ======================== */
/* TABLAS */
.table-container {
  background: rgba(15, 23, 42, 0.5);
  padding: 0;
  border-radius: 16px;
  margin: 20px 0;
  border: var(--glass-border);
  
  /* 🚀 CAMBIO: Altura máxima aumentada a 600px (Mostrará más filas) */
  max-height: 700px; 
  
  /* Mantenido: Habilita el scroll vertical para filas adicionales */
  overflow-y: auto; 
  /* Mantenido: Habilita el scroll horizontal si la tabla es ancha */
  overflow-x: auto; 
  
  width: 100%;
  box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1);
}

.tabla-alumnos {
 /* Necesitas mantener este estilo para que el scroll horizontal funcione */
 min-width: 100%;
 border-collapse: collapse;
}

/* ... (El resto de tus estilos de thead, tbody, etc., permanecen sin cambios) ... */

thead th {
  background: rgba(56, 189, 248, 0.1);
  color: var(--primary);
  padding: 14px 16px;
  text-align: left;
  font-size: 0.85rem;
  text-transform: uppercase;
  letter-spacing: 1px;
  font-weight: 700;
}

tbody td {
  padding: 14px 16px;
  border-bottom: 1px solid rgba(255,255,255,0.05);
  color: #cbd5e1;
  font-size: 0.95rem;
}

tbody tr:hover {
  background: rgba(255,255,255,0.02);
}

/* Estilo para items de clase (Panel Admin) */
.clase-item {
  background: rgba(30, 41, 59, 0.6);
  color: var(--text-main);
  margin: 12px 0;
  padding: 20px;
  border-radius: 16px;
  border: 1px solid rgba(255,255,255,0.05);
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
  transition: transform 0.2s;
}
.clase-item:hover {
  transform: translateX(5px);
  border-color: rgba(255,255,255,0.1);
}

/* ================================
   Overlay Profesional — CBTis 001
   ================================ */

#overlay-ingreso {
  position: fixed;
  left: 50%;
  top: 50%; /* Centrado vertical */
  transform: translate(-50%, -50%) scale(0.9);
  width: 420px;
  max-width: 90%;
  padding: 0;
  border-radius: 24px;
  z-index: 14000; /* Z-Index CRÍTICO más alto */
  
  /* Diseño Tarjeta Glass Premium */
  background: rgba(15, 23, 42, 0.95);
  backdrop-filter: blur(30px) saturate(120%);
  border: 1px solid rgba(255,255,255,0.15);
  box-shadow: 0 50px 100px -20px rgba(0,0,0,0.7), 0 0 0 1px rgba(255,255,255,0.05) inset;
  
  display: grid;
  grid-template-columns: 80px 1fr; /* Estructura original */
  gap: 20px;
  align-items: start;
  opacity: 0;
  pointer-events: none;
  transition: all 0.5s var(--ease-elastic);
  overflow: hidden;
}

/* Barra superior de color */
#overlay-ingreso::before {
  content: "";
  position: absolute;
  top: 0; left: 0; width: 100%; height: 6px;
  background: linear-gradient(90deg, var(--primary), var(--accent));
  z-index: 5;
}

#overlay-ingreso.show {
  opacity: 1;
  transform: translate(-50%, -50%) scale(1);
  pointer-events: auto;
}

/* Contenedor interno para manejar el padding que antes tenía el ID */
.ov-left, .ov-body {
  padding-top: 25px;
  padding-bottom: 10px;
  position: relative;
  z-index: 10;
}

.ov-left { padding-left: 25px; }
.ov-body { padding-right: 25px; }

#overlay-ingreso .ov-left img {
  width: 80px;
  height: 80px;
  object-fit: cover;
  border-radius: 16px;
  border: 2px solid rgba(255,255,255,0.1);
  background: #0f172a;
  box-shadow: 0 10px 20px rgba(0,0,0,0.3);
}

#overlay-ingreso .ov-greet {
  font-size: 0.8rem;
  font-weight: 700;
  color: var(--primary);
  text-transform: uppercase;
  letter-spacing: 1.5px;
  margin-bottom: 5px;
}

#overlay-ingreso .ov-name {
  font-size: 1.3rem;
  font-weight: 700;
  color: #fff;
  margin-bottom: 8px;
  line-height: 1.2;
}

#overlay-ingreso .ov-meta {
  font-size: 0.9rem;
  color: #94a3b8;
  background: rgba(255,255,255,0.05);
  padding: 4px 10px;
  border-radius: 8px;
  display: inline-block;
}

#overlay-ingreso .ov-quote {
  font-size: 0.85rem;
  color: #64748b;
  font-style: italic;
  margin-top: 8px;
  display: block;
}

#overlay-ingreso .ov-actions {
  grid-column: 1 / -1;
  background: rgba(0,0,0,0.2);
  padding: 15px 25px;
  display: flex;
  gap: 10px;
  margin-top: 10px;
  border-top: 1px solid rgba(255,255,255,0.05);
}

#overlay-ingreso .ov-actions button {
  margin: 0;
  flex: 1;
  padding: 10px;
  font-size: 0.9rem;
  box-shadow: none; /* Reset de la sombra grande */
  border: 1px solid rgba(255,255,255,0.1);
  transform: none !important; /* Evitar movimientos extra en el modal */
}

#overlay-ingreso .ov-ok {
  background: var(--primary-dark) !important;
  color: white;
  border: none !important;
}
#overlay-ingreso .ov-ok:hover {
  background: var(--primary) !important;
}

/* Badge estilo píldora */
#overlay-ingreso .badge {
  display: inline-block;
  padding: 4px 8px;
  border-radius: 20px;
  background: rgba(99, 102, 241, 0.2);
  color: #a5b4fc;
  font-size: 0.75rem;
  font-weight: 600;
  border: 1px solid rgba(99, 102, 241, 0.3);
  margin-top: 5px;
}

/* ======================== */
/* OVERLAYS GENERALES (Mensajes) */
.overlay {
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  display: none;
  align-items: center;
  justify-content: center;
  background: rgba(2, 6, 23, 0.6);
  backdrop-filter: blur(8px);
  z-index: 2000; /* Encima de todo menos del ingreso */
  transition: opacity 0.3s ease;
}

.overlay-content {
  background: #1e293b;
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 20px;
  padding: 30px;
  text-align: center;
  box-shadow: 0 25px 50px -12px rgba(0,0,0,0.6);
  min-width: 280px;
  max-width: 400px;
  animation: popIn 0.3s var(--ease-elastic);
  color: #f8fafc;
}

/* Colores de borde superior */
.overlay.success .overlay-content { border-top: 5px solid #10b981; }
.overlay.error .overlay-content   { border-top: 5px solid #ef4444; }
.overlay.warning .overlay-content { border-top: 5px solid #f59e0b; }
.overlay.info .overlay-content    { border-top: 5px solid #3b82f6; }

/* ======================== */
/* VIDEO Y QR (FRAME TECNOLÓGICO) */
#video-container, #qr-reader {
  width: 100%;
  max-width: 400px;
  height: 300px;
  border-radius: 20px;
  overflow: hidden;
  margin: 25px auto;
  background: #000;
  border: 1px solid rgba(255,255,255,0.1);
  box-shadow: 0 20px 40px -10px rgba(0,0,0,0.5);
  position: relative;
}

/* Línea de escaneo animada */
#qr-reader::after {
  content: '';
  position: absolute;
  top: 0; left: 0; width: 100%; height: 2px;
  background: var(--primary);
  box-shadow: 0 0 20px 2px var(--primary);
  animation: scanLine 2s infinite cubic-bezier(0.4, 0, 0.2, 1);
  opacity: 0.8;
  z-index: 50;
}

#qr-reader video, #video-container video {
  width: 100% !important;
  height: 100% !important;
  object-fit: cover;
}

#alumno-qrcode, #prof-qrcode {
  margin: 25px auto;
  width: 200px;
  height: 200px;
  padding: 15px;
  background: #fff;
  border-radius: 16px;
  box-shadow: 0 0 40px rgba(255,255,255,0.1);
  animation: popIn 0.5s var(--ease-elastic);
}

/* ======================== */
/* MINI-OVERLAY (Lista flotante) */
#overlay-mini {
  position: fixed;
  bottom: 20px;
  right: 20px;
  width: 320px;
  background: rgba(15, 23, 42, 0.9); /* Fondo oscuro sólido */
  border-radius: 16px;
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.6);
  color: #f1f5f9;
  display: none;
  flex-direction: column;
  padding: 20px;
  backdrop-filter: blur(16px);
  border: 1px solid rgba(255,255,255,0.1);
  z-index: 1100; /* Encima del contenido base */
  animation: slideLeft 0.4s var(--ease-elastic);
}

#overlay-mini ul {
  list-style: none;
  padding: 0;
  margin: 10px 0;
  max-height: 240px;
  overflow-y: auto;
}

#overlay-mini li {
  padding: 10px 0;
  border-bottom: 1px solid rgba(255,255,255,0.1);
  font-size: 0.9rem;
  color: #cbd5e1;
}

#overlay-mini button {
  width: 100%;
  margin-top: 10px;
  background: rgba(59, 130, 246, 0.2);
  color: #60a5fa;
  border: 1px solid rgba(59, 130, 246, 0.3);
  box-shadow: none;
}

/* ======================== */
/* OVERLAY CLASE V3 (Modal Admin) */
.overlay-clase-v3 {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.8);
  backdrop-filter: blur(5px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999; /* Muy alto */
}

.overlay-clase-v3 .overlay-content {
  background: #1e293b;
  width: 320px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  text-align: left;
}

.overlay-clase-v3 label {
  font-weight: 600;
  color: var(--text-muted);
  font-size: 0.9rem;
}

.overlay-clase-v3 select,
.overlay-clase-v3 input {
  background: #0f172a;
  border: 1px solid #334155;
  color: white;
  padding: 10px;
  margin: 0; /* Reset margin */
  width: 100%;
}

.overlay-botones {
  display: flex;
  gap: 10px;
  margin-top: 10px;
}
.overlay-botones button {
  flex: 1;
  margin: 0;
  box-shadow: none;
}
.overlay-botones button:first-child {
  background: #10b981;
  color: white;
  border: none;
}
.overlay-botones button:last-child {
  background: #ef4444;
  color: white;
  border: none;
}

/* ======================== */
/* ANIMACIONES */
@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
@keyframes fadeSlideIn { from { opacity: 0; transform: translateY(40px) scale(0.9); } to { opacity: 1; transform: translateY(0) scale(1); } }
@keyframes popIn { from { opacity: 0; transform: scale(0.8); } to { opacity: 1; transform: scale(1); } }
@keyframes slideLeft { from { opacity: 0; transform: translateX(50px); } to { opacity: 1; transform: translateX(0); } }
@keyframes scanLine {
  0% { top: 0%; opacity: 0; }
  20% { opacity: 1; }
  80% { opacity: 1; }
  100% { top: 100%; opacity: 0; }
}

/* ======================== */
/* RESPONSIVE */
@media (max-width: 650px) {
  section { padding: 25px 20px; width: 90%; }
  h1 { font-size: 1.8rem; }
  input { width: 100%; }
  #video-container, #qr-reader { height: 260px; }
  #overlay-mini { width: 90%; right: 5%; bottom: 20px; }
  
  /* Ajuste móvil overlay ingreso */
  #overlay-ingreso {
    grid-template-columns: 1fr;
    text-align: center;
    gap: 10px;
    width: 92%;
  }
  #overlay-ingreso .ov-left { padding: 20px 20px 0 20px; display: flex; justify-content: center; }
  #overlay-ingreso .ov-body { padding: 0 20px 10px 20px; }
  #overlay-ingreso .ov-actions { justify-content: center; }
}




// ========================
// Contraseñas por rol
// ========================
const passwords = { Profesor: "prof123", Admin: "admin123" };
let rolActual = null;

// ========================
// Selección de rol
// ========================
document.querySelectorAll(".rol-btn").forEach(btn => {
  btn.addEventListener("click", () => {
    rolActual = btn.dataset.rol;
    if (rolActual === "Alumno") {
      overlayRol.style.display = "none";
      abrirPanelRol("Alumno");
    } else {
      passwordContainer.style.display = "block";
      errorRol.textContent = "";
      passwordRol.value = "";
    }
  });
});

btnEntrar.addEventListener("click", () => {
  if (passwordRol.value === passwords[rolActual]) {
    overlayRol.style.display = "none";
    abrirPanelRol(rolActual);
  } else {
    errorRol.textContent = "❌ Contraseña incorrecta";
  }
});

passwordRol.addEventListener("keypress", (e) => {
  if (e.key === "Enter") btnEntrar.click();
});

// ========================
// Abrir panel según rol
// ========================
function abrirPanelRol(rol){
  panelAlumno.style.display = "none";
  panelProfesor.style.display = "none";
  panelAdmin.style.display = "none";

  if(rol==="Alumno") panelAlumno.style.display = "block";
  if(rol==="Profesor"){
    panelProfesor.style.display = "block";
    activarModoCamara(); // ahora usa QrScanner
  }
  if(rol==="Admin") panelAdmin.style.display = "block";
}